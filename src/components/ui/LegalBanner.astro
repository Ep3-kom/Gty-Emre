---
const isDev = import.meta.env.DEV;
---

{isDev && (
  <div class="my-8 border border-accent/40 bg-accent/5 px-5 py-4 text-small">
    <p class="mono-eyebrow text-accent">TODO klant — alleen zichtbaar in dev</p>
    <p class="mt-2 text-ink">
      Deze juridische tekst is een placeholder. De klant moet de definitieve, juridisch
      gecontroleerde tekst aanleveren. In productie wordt deze banner niet getoond.
    </p>
  </div>
)}
